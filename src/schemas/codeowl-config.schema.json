{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://codeowl.dev/schemas/codeowl-config.schema.json",
  "title": "CodeOwl Configuration",
  "description": "Configuration schema for .codeowl/config.json",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "model": {
      "type": "string",
      "description": "Primary analysis model in provider/model format (e.g. anthropic/claude-sonnet-4-5)",
      "examples": ["anthropic/claude-sonnet-4-5", "openai/gpt-4o"]
    },
    "transport": {
      "type": "string",
      "description": "Optional transport used to send requests. Use this when the model name represents the routed provider/model but the HTTP transport should be a gateway such as Vercel AI Gateway.",
      "examples": ["auto", "vercel", "openrouter", "openai"]
    },
    "apiBaseUrl": {
      "type": "string",
      "description": "Optional OpenAI-compatible base URL for custom gateways or proxies.",
      "examples": ["https://ai-gateway.vercel.sh/v1", "https://my-gateway.example.com/v1"]
    },
    "exclude": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Additional paths/patterns to exclude from analysis",
      "examples": [["generated", "vendor", "proto"]]
    },
    "disabledReviewers": {
      "type": "object",
      "description": "Per-mode list of reviewer IDs to disable.",
      "properties": {
        "audit": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Reviewer IDs to disable for audit mode.",
          "examples": [["ui-component", "responsiveness"]]
        },
        "prReview": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Reviewer IDs to disable for PR review mode.",
          "examples": [["inline-docs", "license-compliance"]]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
