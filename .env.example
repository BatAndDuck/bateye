# API key for direct provider access (Anthropic, OpenAI, OpenRouter, Google, etc.)
CODE_OWL_LLM_MODEL_API_KEY=your-provider-api-key

# Vercel AI Gateway credentials (required only when using vercel/* models)
AI_GATEWAY_API_KEY=your-vercel-ai-gateway-api-key
VERCEL_OIDC_TOKEN=your-vercel-oidc-token

# GitHub token — required to post PR comments when running codeowl pr-review locally
GITHUB_TOKEN=your-github-token

# Repository and PR to review (used by the pr-review command; set automatically in CI)
GITHUB_REPOSITORY=owner/repo
PR_NUMBER=123
